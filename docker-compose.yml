services:
  myagentwatch:
    build: .
    ports:
      - "10000:10000"
    volumes:
      - ~/.local/share/opencode:/data/opencode:ro
      - ./config.yaml:/app/config.yaml
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - MYAGENTWATCH_PORT=10000
